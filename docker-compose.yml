version: '3.3'

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        image: docker.pkg.github.com/felixfong227/docker-swarm-express-playground/node-expressjs:latest

        depends_on:
            - redis

        ports:
            - "8080:8080"
    
    redis:
        image: redis